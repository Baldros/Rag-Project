# Knowledge base — ETL e retrieval para agentes (via MCP)
#
# torch é instalado manualmente por causa do CUDA. Nesta máquina:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126

# Parsing de documentos
docling>=2.95.0
pypdf>=6.11.0

# Persistência
chromadb>=1.5.0          # índice vetorial (camada descartável)
# SQLite + FTS5 vêm da stdlib: estrutura, BM25 e jobs

# Modelos de recuperação (rodam localmente na GPU, via torch)
sentence-transformers>=6.0.0   # bge-m3 (embedding) e bge-reranker-v2-m3 (rerank)

# Superfície de contato
mcp>=2.2.0

# O LLM do Pass 3 (resumos) fala com o Ollama por HTTP, usando o httpx que já
# vem com o mcp — nenhum pacote a mais. O Ollama em si é um serviço externo:
#   ollama pull qwen3:8b
# Sem ele a ingestão conclui os Passes 1 e 2 e deixa os resumos pendentes.
#
# Não há langchain nem llama-index: a geração de resposta é do agente que
# consome o MCP, e o resumo é do LLM local.
